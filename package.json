{
  "name": "capsule",
  "private": true,
  "version": "0.0.0",
  "homepage": "https://rishisingh1034.github.io/capsule",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d frontend/dist"
  },
  "dependencies": {
    "react-responsive": "^10.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
